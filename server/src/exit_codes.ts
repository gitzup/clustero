export const EXIT_NODEMON_REFRESH = 0; // exit as success for Nodemon so it doesn't crash
export const EXIT_USER_INTERRUPT = 0; // exit as success for user

let counter = 0;
export const EXIT_GENERAL_ERROR = ++counter;
